/**
 * Retrieves data from the EE API based on the supplied type and query
 */
module.exports = {
  async find({ type, query }) {
    // do stuff
    return {
      total: 0,
      results: [],
    };
  },
};
